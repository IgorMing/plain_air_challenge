// ideally, these variables would be placed into a theme file,
// sharing through the entire project not just colors,
// but measures, fonts, etc.

export const SPACING = 20;
export const PRIMARY_COLOR = '#0b8ce9';
